# Files git ignores that Claude Code copies into each new worktree.
# Files only (never directories like node_modules/): run `bun install` in the worktree.

# Secrets and flags, shared across worktrees; .env.local is per-worktree and
# generated by `bun run worktree:setup`, never copied — docs/adr/0049.
.env

# Claude Code local permissions
.claude/settings.local.json
