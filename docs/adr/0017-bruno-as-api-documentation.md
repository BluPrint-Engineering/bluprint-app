# Bruno collection as the API documentation

The API is documented as a Bruno collection of `.bru` files in `apps/api/bruno/`: versioned in git, reviewable in PRs, with no cloud account or paid sync. Every new route enters the collection in the same PR that creates it.
