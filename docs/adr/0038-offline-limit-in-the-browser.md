# A conscious limit on offline in the browser

The local queue exists, but iPhone Safari does not sync with the app closed and may discard local storage after days without use. So the queue uploads when the assistant reopens the app with signal, and the interface shows what is pending. Full background offline is one of the reasons for a native app later.

Requirements: RF-511, RNF-12, RNF-21
