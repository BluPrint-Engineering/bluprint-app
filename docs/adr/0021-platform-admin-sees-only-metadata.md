# The platform admin never sees project content

The platform admin sees organizations, licenses and project counts, and nothing inside a project: no plans, pins, photos, reports or unit data. The barrier exists for LGPD compliance.

Requirements: RF-105, RNF-14
