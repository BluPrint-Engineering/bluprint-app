meta {
  name: Health check
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/health?verbose=true
  body: none
  auth: none
}

params:query {
  verbose: true
}

assert {
  res.status: eq 200
  res.body.status: eq ok
  res.body.database: eq up
  res.body.verbose.runtime: contains node
}
