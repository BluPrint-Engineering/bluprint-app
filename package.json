{
	"name": "bluprint-app",
	"private": true,
	"packageManager": "bun@1.4.0",
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"prepare": "lefthook install",
		"dev": "bun run --filter @bluprint/shared build && bun run --parallel --filter '*' --if-present dev",
		"build": "bun run --filter @bluprint/shared build && bun run --filter './apps/*' --if-present build",
		"test": "bun run --filter @bluprint/shared build && bun run --filter '*' --if-present test",
		"test:unit": "bun run --filter @bluprint/shared build && bun run --filter '*' --if-present test:unit",
		"test:int": "bun run --filter @bluprint/shared build && bun run --filter '*' --if-present test:int",
		"typecheck": "bun run --filter @bluprint/shared build && bun run --filter '*' --if-present typecheck",
		"lint": "bun run --filter @bluprint/shared build && biome check . && bun run --filter @bluprint/api lint",
		"lint:fix": "bun run --filter @bluprint/shared build && biome check --write . && bun run --filter @bluprint/api lint:fix",
		"format": "biome format --write . && bun run --filter @bluprint/api format"
	},
	"devDependencies": {
		"@biomejs/biome": "2.5.11",
		"lefthook": "2.1.12",
		"typescript": "7.0.2"
	}
}
